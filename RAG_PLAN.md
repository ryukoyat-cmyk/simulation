# RAG 시나리오 연결 전략

## 핵심 원칙

STEP1의 페르소나와 STEP2의 민원 상황은 별도의 개념으로 분리한다.

- STEP1 페르소나: STEP3 대화에서 학부모가 말하는 방식, 정서, 문제 인식 방식을 정한다.
- STEP2 민원 상황: 사례 자료에서 독립적으로 생성한다.
- 민원 상황 생성 API에는 STEP1에서 선택한 페르소나 값을 보내지 않는다.

## 목표

`초등학교 학부모 교권침해 민원 사례 모음집` PDF를 기반으로, 주사위를 누를 때마다 다양한 연구용 학부모 민원 상황을 생성한다.

## 현재 구현

- 원문 표지 기준 사례 수: 2077건
- PDF 자동 추출 코퍼스: 1914개 사례 후보
- 추가 변주 요소(+a): 민원 관점, 주제 다양화, 비식별화 재구성 규칙
- STEP2 주사위 버튼은 `/api/random-situation`을 호출한다.
- `/api/random-situation`은 STEP1 페르소나를 받지 않는다.

## 생성 기준

난이도는 사용하지 않는다. 상황 생성 기준은 아래로 제한한다.

- 민원 주제
- 사례 근거
- 최근 생성 사례와의 중복 회피
- 연구용 비식별화 및 순화 재구성

## 사용자 흐름

1. 사용자가 STEP1에서 페르소나를 선택한다.
2. 사용자가 STEP2 민원 상황 선택 페이지에서 주사위를 누른다.
3. Netlify Function이 사례 코퍼스에서 최근 사용 사례를 제외하고 여러 주제의 사례 조각을 선택한다.
4. OpenAI API가 선택된 사례 조각을 바탕으로 새로운 민원 상황을 비식별화하여 재구성한다.
5. 생성된 상황을 STEP2 입력창에 표시한다.
6. STEP3에서는 선택된 페르소나가 학부모 대화 방식에만 적용된다.

## 다음 단계

Supabase pgvector를 붙일 경우에도 페르소나 독립 원칙은 유지한다.

### complaint_cases

- `id`
- `source_file`
- `page_start`
- `case_title`
- `case_text`
- `category`
- `embedding`
- `created_at`

### generated_scenarios

- `id`
- `case_ids`
- `category`
- `situation`
- `created_at`

## 생성 규칙

- 실제 인명, 학교명, 지역명, 날짜, 연락처 등은 제거하거나 일반화한다.
- 원문 사례를 그대로 복사하지 않고, 연구용 시뮬레이션 상황으로 재구성한다.
- 자녀 이름은 `"금쪽이"`로 고정한다.
- 선택된 페르소나의 말투나 정서를 민원 상황 생성에 반영하지 않는다.
- 난이도 표현은 사용하지 않는다.
- 상황은 1~2문장으로 작성한다.

## API 설계

### `/api/random-situation`

요청:

```json
{
  "excludeIds": ["case-0001", "case-0120"]
}
```

응답:

```json
{
  "situation": "금쪽이가 쉬는 시간 친구와 다툰 뒤 불안해하고 있다는 이유로, 학부모가 담임교사에게 사실 확인과 재발 방지 안내를 요구하는 상황입니다.",
  "topic": "학교폭력/교우관계",
  "sourceIds": ["case-0003", "case-0188", "case-0420"],
  "generatedBy": "openai-case-corpus"
}
```
