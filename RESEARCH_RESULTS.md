# AI 기반 음성 시뮬레이션 프로그램 설계 및 개발 결과

> 본 문서는 「예비교원의 학부모 민원 대응 역량 강화를 위한 AI 기반 음성 시뮬레이션 프로그램」의 설계 및 개발 결과를 기술한 연구 보고이다. 기술 내용은 현재 저장소에 구현·배포된 최종 프로그램(`index.html`, `assets/app.js`, `netlify/functions/*`, `supabase.sql`)을 근거로 작성되었다.

---

## Ⅰ. 프로그램 설계 방향 및 전체 구조

### 1. 설계의 배경과 문제의식

교원의 학부모 민원 대응 역량은 실제 민원 상황에 반복적으로 노출되면서 형성되는 실천적 역량이다. 그러나 실제 민원 상황은 (가) 사전에 예측하거나 통제할 수 없고, (나) 실패의 비용이 교원 개인과 학생 모두에게 전가되며, (다) 예비교원에게는 노출 자체가 구조적으로 차단되어 있다는 세 가지 제약을 갖는다. 이로 인해 기존의 민원 대응 연수는 사례 강의와 문서화된 대응 지침 전달에 머무르는 경우가 많았으며, 학습자가 실제로 '말하고, 그 말의 결과를 관찰하고, 수정하는' 수행 수준의 훈련은 이루어지기 어려웠다.

본 프로그램은 이 간극을 해소하기 위해, 실제 민원 사례 코퍼스에 기반한 가상 학부모와 **음성으로 대화하고**, 그 수행을 **관찰 가능한 준거에 따라 평가받는** 시뮬레이션 학습 환경을 설계·개발하는 것을 목적으로 하였다.

### 2. 설계 원리

프로그램은 다음 네 가지 학습설계 원리를 구현 수준의 요구사항으로 번역하여 설계되었다.

| 설계 원리 | 이론적 근거 | 구현상의 번역 |
| --- | --- | --- |
| 진정성 있는 맥락(authentic context) | 상황학습(situated learning) | 실제 민원 사례 2,077건 코퍼스 기반 시나리오 생성, 학교급별 맥락 분기 |
| 안전한 실패 공간(safe-to-fail space) | 시뮬레이션 기반 학습 | 가상 학부모, 재도전 기능, 결과의 비공식성 명시 |
| 즉각적·근거 기반 피드백 | 형성평가(formative assessment) | 매 발화 직후 즉시 피드백 + 종료 후 총괄평가의 이원 구조 |
| 수행의 가시화(making performance visible) | 인지적 도제(cognitive apprenticeship) | 14개 수행요소별 대화 근거(evidence) 제시, 대화 전문 병기 |

### 3. 핵심 설계 결정

프로그램 전반을 규율하는 네 가지 상위 설계 결정은 다음과 같다.

**① 페르소나-시나리오 직교(orthogonality) 원칙.** 학부모의 '성격'과 민원의 '사건'을 서로 독립된 설계 차원으로 분리하였다. 시나리오 생성 API(`/api/random-situation`)는 사용자가 선택한 페르소나 값을 **입력받지 않는다**. 이 결정에 의해 동일한 민원 사건을 다섯 가지 상이한 정서적·의사소통적 조건에서 반복 연습할 수 있고, 역으로 동일한 페르소나를 무한한 사건 조건에 대응시킬 수 있다. 이는 학습자가 '사건 유형별 매뉴얼'이 아니라 '관계적 대응 원리'를 학습하도록 유도하기 위한 장치이다.

**② 음성 우선(voice-first) 상호작용.** 민원 대응의 실제 수행 국면은 대면 상담과 전화 통화이며, 이 국면의 핵심 부담은 문장 작성이 아니라 **즉시성**에 있다. 따라서 기본 입력을 음성으로 설정하고, 텍스트 입력은 미지원 환경을 위한 대체 경로로 배치하였다.

**③ 서버 매개(server-mediated) 아키텍처.** 모든 AI 호출은 서버리스 함수를 경유한다. 이는 API 키의 클라이언트 노출을 원천 차단하고, 프롬프트·평가 준거·비식별화 규칙을 서버 측 통제 하에 두어 연구 도구로서의 일관성을 확보하기 위한 결정이다.

**④ 다층 성능저하 감내(graceful degradation).** 학습 활동은 외부 AI 서비스의 일시적 실패로 중단되어서는 안 된다. 모든 실패 경로에 대해 학습 지속을 우선하는 대체 동작을 설계하였다.

### 4. 전체 시스템 구조

프로그램은 4개 계층으로 구성된 서버리스 웹 애플리케이션이다.

```
┌─────────────────────────────────────────────────────────────┐
│  [프레젠테이션 계층] 정적 클라이언트                          │
│  index.html · assets/app.js(482 LOC) · assets/styles.css     │
│  - 단일 상태 객체 S 기반 SPA, 7개 화면 렌더 함수              │
│  - Web Speech API(STT) · Audio/SpeechSynthesis(재생)         │
│  - html2canvas + jsPDF (결과 PDF 내보내기)                    │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTPS / JSON
┌───────────────────────────┴─────────────────────────────────┐
│  [애플리케이션 계층] Netlify Functions (Node.js, ESM)         │
│  /api/chat            학부모 발화 생성(첫 발화·후속 턴)        │
│  /api/speak           페르소나별 음성 합성(TTS)               │
│  /api/random-situation 사례 기반 민원 시나리오 생성            │
│  /api/turn-feedback   턴 단위 형성적 피드백                   │
│  /api/evaluate        14요소 총괄 평가                        │
│  /api/save-message · /api/save-situation  기록 저장(확장 지점) │
│  /api/realtime-session  실시간 음성 세션 발급(확장 지점)       │
│  /api/health          운영 진단                               │
│  _shared.js           공통 호출·오류·재시도·저장 유틸(230 LOC) │
│  _complaint_cases.js  비식별 민원 사례 코퍼스(2,077건)         │
└───────────┬───────────────────────────────┬─────────────────┘
            │                               │
┌───────────┴──────────────┐   ┌────────────┴────────────────┐
│ [AI 서비스 계층]          │   │ [데이터 계층] Supabase       │
│ Chat Completions (대화·   │   │ simulation_messages          │
│  시나리오·피드백·평가)    │   │ simulation_evaluations       │
│ Audio Speech (TTS)        │   │ simulation_situations        │
└───────────────────────────┘   └─────────────────────────────┘
```

### 5. 학습자 경험 흐름

프로그램은 6단계 선형 흐름으로 구조화되며, 각 단계는 독립된 화면 상태로 관리된다.

| 단계 | 화면 | 학습 활동 | 설계 의도 |
| --- | --- | --- | --- |
| 0 | 타이틀 | 연구 참여·개인정보 고지 확인 | 연구윤리 고지, 평가의 위상 사전 명시 |
| 1 | STEP 01 | 교원 유형 선택(예비교원/현직교원) | 대응 범위 기대수준의 조정 |
| 2 | STEP 02 | 학교급 선택(초/중/고) | 민원 쟁점 맥락의 분기 |
| 3 | STEP 03 | 학부모 유형 선택(5종) | 상호작용 조건 설정 |
| 4 | STEP 04 | 민원 상황 생성 또는 직접 입력 | 사건 조건 설정, 학습자 주도 시나리오 허용 |
| 5 | 시뮬레이션 | 음성 대화 + 실시간 즉시 피드백 | 수행 및 형성평가 |
| 6 | 결과 | 14요소 평가·근거·강약점·대화 전문 | 총괄평가 및 성찰 |

전체 조건 조합은 **교원 유형 2 × 학교급 3 × 페르소나 5 = 30개 상호작용 조건**이며, 여기에 사례 코퍼스 기반으로 생성되는 비반복 시나리오가 결합되어 실질적으로 반복 학습의 상한이 존재하지 않는 구조를 이룬다.

---

## Ⅱ. 학부모 민원 시나리오 및 페르소나 설계 결과

### 1. 민원 사례 코퍼스 구축

시나리오의 진정성을 확보하기 위해 실제 민원 사례 자료집(『초등학교 학부모 교권침해 민원 사례 모음집』, 총 465면)을 원자료로 하여 기계 판독 가능한 사례 코퍼스를 구축하였다.

**추출 파이프라인**(`tools/extract_complaint_cases.py`)은 다음 절차로 구성된다.

1. PDF 텍스트 계층 추출(`pypdf`)
2. 반복 머리글·면 번호 등 잡음 패턴 제거(정규식 기반)
3. 사례 경계 탐지 및 제목/본문 분리
4. 9개 규칙 집합에 의한 주제 자동 분류(키워드 매칭 최대 점수)
5. `{id, page, topic, title, excerpt}` 구조로 정규화하여 JS 모듈로 직렬화

**구축 결과**, 원자료 표지 기준 총계와 동일한 **2,077건**의 사례가 추출되었으며(추출률 100%), 주제 분포는 다음과 같다.

| 주제 범주 | 사례 수 | 비율 |
| --- | ---: | ---: |
| 연락/근무시간 | 715 | 34.4% |
| 학교폭력/교우관계 | 416 | 20.0% |
| 수업방해/생활지도 | 342 | 16.5% |
| 폭언/협박 | 204 | 9.8% |
| 기타 민원 | 182 | 8.8% |
| 학습/평가 | 73 | 3.5% |
| 안전/보건 | 48 | 2.3% |
| 방과후/하교 | 33 | 1.6% |
| 특수/지원 | 33 | 1.6% |
| 급식/물품 | 31 | 1.5% |
| **계** | **2,077** | **100.0%** |

주목할 점은 '연락/근무시간'(34.4%)이 최다 범주라는 사실로, 이는 민원의 상당 부분이 사건의 경중이 아니라 **교사의 가용성과 응답 경계**를 둘러싸고 발생함을 시사한다. 이 분포는 평가 준거에 '경계 설정', '대응 범위 설정', '대응 중단 판단' 요소를 포함시킨 근거가 되었다.

### 2. 시나리오 생성 절차 설계

시나리오는 원문 사례를 직접 제시하지 않고, 복수 사례를 재료로 한 **비식별 재구성** 방식으로 생성된다(`/api/random-situation`).

**① 사례 표집.** 최근 사용 사례 ID(최대 120건)와 최근 주제(최대 8건)를 제외한 풀에서 4건을 표집하되, 초기 2건은 서로 다른 주제에서 선택되도록 제약하여 주제 편향을 완화하였다. 표집에는 암호학적 난수(`crypto.getRandomValues`)를 사용하였다.

**② 생성 관점(angle) 부여.** 동일 사례 조합이라도 상이한 시나리오가 산출되도록 10개 생성 관점 중 하나를 무작위 결합한다: 사실 확인 요구, 즉각 조치 요구, 교사의 설명 방식에 대한 불만, 가정과 학교의 책임 경계 논의, 친구 관계 갈등 중재 요구, 생활지도 결과에 대한 이의, 연락 시각과 응답 속도에 대한 불만, 학습 또는 평가 결과에 대한 이의, 안전 또는 보건 조치에 대한 문제 제기, 학교 절차를 잘 모르는 상태에서의 강한 요구.

**③ 학교급 맥락 주입.** 학교급별로 상이한 쟁점 영역을 명시적으로 제공한다.

| 학교급 | 주입되는 맥락 |
| --- | --- |
| 초등학교 | 쉬는 시간, 급식, 준비물, 친구 관계, 돌봄·방과후, 담임교사와의 일상 소통 |
| 중학교 | 단체 채팅방, 수행평가, 생활지도, 친구 관계 갈등, 동아리·체육활동, 담임·교과교사 간 확인 |
| 고등학교 | 내신 평가, 진로·진학, 출결, 수행평가 공정성, 수업 태도 지도, 학교생활기록부 관련 민감성 |

**④ 이중 표상(dual representation) 산출.** 생성 결과는 두 개의 서로 다른 텍스트로 산출된다. 이는 본 설계의 특징적 요소이다.

- `situation` — **학습자 화면 표시용 2문장**. 1문장에 언제/어디서/무엇이 있었는지, 2문장에 학부모의 요구를 담는다. 학습자가 대화 시작 시점에 알 수 있는 정보의 범위를 규정한다.
- `situationContext` — **AI 학부모 전용 내부 맥락 4~6문장**. 사건 배경, 학부모가 전해 들은 정보, 교사가 확인해야 할 쟁점, 가능한 절차를 포함한다.

이 비대칭 구조에 의해, AI 학부모는 학습자가 모르는 배경 정보를 보유한 상태로 대화를 전개하게 된다. 그 결과 학습자는 **질문을 통해 사실을 능동적으로 확보해야만** 대화를 진전시킬 수 있으며, 이는 평가 준거의 '사실 확인' 요소가 실제로 관찰 가능해지는 조건을 형성한다.

**⑤ 비식별화 및 순화 규칙.** 프롬프트 수준과 후처리 수준에서 이중으로 적용된다.

- 원문 사례의 직접 복사 금지, 개인정보·고유 사건 정보 제거
- 학생 명칭은 `금쪽이`로 고정(`ensureChildName` 후처리로 강제)
- 학교명·지역명·교직원 실명·연락처·병명 사용 금지
- 욕설·협박·폭력 표현의 교육적 순화
- 난이도 표현 미사용(학습자 자기효능감에 대한 선입견 배제)

**⑥ 실패 대비.** AI 생성 실패 시 학교급별 사전 작성 시나리오 2종(총 6종)으로 대체하여, 학습 활동이 중단되지 않도록 하였다.

### 3. 학부모 페르소나 설계

페르소나는 민원 상황에서 나타나는 학부모의 의사소통 양식을 유형화한 것으로, 총 5종을 설계하였다. 각 페르소나는 **9개 속성 차원**으로 구조화되어 정의되며, 이 구조화가 본 설계의 핵심 기여에 해당한다.

**페르소나 정의 차원**

| 차원 | 정의 | 설계 기능 |
| --- | --- | --- |
| `concern` | 핵심 관심사 | 대화의 목적 지향점 결정 |
| `emotion` | 감정 강도 및 변화 양상 | 정서적 부하 수준 조절 |
| `style` | 자주 쓰는 말투(예시 발화 포함) | 어휘·화행 수준의 일관성 확보 |
| `questions` | 질문 방식 | 학습자에게 요구되는 응답 유형 규정 |
| `repeat` | 반복 행동 | 불충분한 응답에 대한 압력 기제 |
| `escalate` | **갈등 고조 조건** | 학습자 발화의 부적절성에 대한 반응 규칙 |
| `settle` | **안정 조건** | 학습자 발화의 적절성에 대한 반응 규칙 |
| `boundaries` | 허용·비허용 표현 및 경계 반응 | 윤리적 안전장치, 경계 설정 학습 조건 |
| `closing` | 대화 종료 반응 | 종료 국면의 자연스러움 확보 |

이 중 `escalate`와 `settle`은 페르소나를 **정적 성격 기술이 아닌 반응 함수**로 만드는 핵심 요소이다. 즉 AI 학부모의 감정 상태는 고정된 것이 아니라 학습자의 직전 발화에 대한 함수로 변화하며, 이를 통해 학습자는 자신의 언어 선택이 갈등의 경로를 바꾼다는 사실을 직접 경험하게 된다.

**설계된 5개 페르소나 요약**

| 유형 | 핵심 관심사 | 감정 강도 | 갈등 고조 조건 | 안정 조건 |
| --- | --- | --- | --- | --- |
| **협력형** | 공정한 이해와 재발 방지 계획의 합의 | 차분하나 사실 누락 시 단호 | 말 바뀜, 근거 없는 단정, 책임 회피 | 감정 인정 + 사실 확인 + 실행 가능한 후속 일정 |
| **걱정형** | 자녀의 정서적·관계적 안전 | 불안 높음, 반복 확인 | 감정 무시, 근거 없는 안심("걱정 마세요") | 감정 인정 후 구체 사실·확인 계획·연락 시점 |
| **회피형** | 누적된 불신, 책임의 희석 우려 | 초기 냉담·단답, 안전감 시 개방 | 변명, 탓 돌리기, 성급한 결론, 감정 축소 | 판단 없는 경청 + 책임 있는 다음 절차 |
| **요구형** | 기준의 공정성, 조치 범위, 공식 절차 | 단호·논리적 | 불명확한 답, 과도한 약속 후 번복 | 가능·불가능 범위와 공식 절차의 명확한 안내 |
| **압박형** | 지연 없는 즉시 처리 | 초반 강도 높음, 발화 속도 빠름 | 방어적 반박, 감정 맞대응, 절차 없는 지연 | 차분한 경청 + 명확한 경계 + 공식 회신 절차 |

**윤리적 안전장치.** 전 페르소나에 공통으로 욕설·협박·혐오 표현의 생성을 금지하였다. 특히 압박형에 대해서는 "욕설·협박은 하지 않는다. 교사가 정중하게 한계를 알리면 강도를 낮추고 공식 절차로 전환한다"는 경계 규칙을 명시하였다. 이는 두 가지 목적을 갖는다. 첫째, 학습자에게 과도한 정서적 부담을 주지 않는다. 둘째, 더 중요하게는 **경계 설정이라는 수행이 실제로 효과를 갖는 환경**을 보장한다. 어떤 대응으로도 완화되지 않는 학부모는 학습 자극으로서 기능하지 못하기 때문이다.

**역할 고정 장치.** LLM 기반 역할 연기에서 빈번한 실패 양상은 모델이 역할을 이탈하여 사용자에게 조언하거나 상황을 3인칭으로 해설하는 것이다. 이를 방지하기 위해 시스템 프롬프트에 다음 제약을 중첩 적용하였다.

- "당신은 언제나 학부모 역할만 유지합니다. 절대로 교사가 되거나, 교사에게 조언·평가·수업지시·역할 안내를 하지 마세요."
- "'학부모는', '학부모가', '상황은' 같은 해설문을 쓰지 않습니다."
- "현재 민원 상황을 바꾸거나 새 사건을 만들지 마세요. 상황에 없는 쟁점을 새로 만들지 마세요."

특히 **첫 발화 생성은 별도 엔드포인트 로직으로 분리**하였다. 첫 발화는 화면 표시와 동시에 음성으로 재생되므로, 상황 설명문을 요약 낭독하면 대사가 아닌 지문처럼 들리는 문제가 관찰되었기 때문이다. 이에 첫 발화 전용 프롬프트에 "지금은 학부모가 교사에게 전화를 걸어 첫 마디를 꺼내는 순간", "민원 내용을 처음부터 빠짐없이 늘어놓는 사람은 없습니다", "귀로 들을 때 실제 통화처럼 들려야 합니다" 등의 지시와 함께 **긍정 예시·부정 예시를 병기**하여 구어체 발화 품질을 확보하였다.

---

## Ⅲ. 음성 시뮬레이션 상호작용 설계 결과

### 1. 음성 상호작용 구조

음성 대화 루프는 다음 순환 구조로 설계되었다.

```
[대화 시작]
   ↓
학부모 첫 발화 생성 (/api/chat, initial=true)
   ↓
음성 합성 및 재생 (/api/speak → 실패 시 브라우저 TTS → 실패 시 텍스트)
   ↓
교사 음성 입력 (Web Speech API, ko-KR)  ←──────────┐
   ↓                                                │
전송 → 즉시 피드백 요청(병렬) + 학부모 응답 요청     │
   ↓                                                │
학부모 응답 음성 재생 ───────────────────────────────┘
   ↓ (교사 발화 4회 이상)
[대화 종료 및 평가]
```

### 2. 음성 출력: 페르소나별 정서 표현 설계

음성 합성에는 낭독 지시문(`instructions`)을 지원하는 TTS 모델(`gpt-4o-mini-tts`)을 채택하였다. 이는 **동일한 텍스트라도 페르소나에 따라 다른 정서로 들리도록** 하기 위한 결정이며, 텍스트 수준의 페르소나 차별화를 청각 수준까지 확장한다.

| 페르소나 | 낭독 지시문 |
| --- | --- |
| 협력형 | 정중하고 차분한 중년 학부모의 목소리로, 또박또박 예의 있게 말합니다. |
| 걱정형 | 걱정이 묻어나는 조심스러운 목소리로, 살짝 떨리듯 천천히 말합니다. |
| 회피형 | 감정을 눌러 담은 낮고 짧은 말투로, 거리감 있게 담담히 말합니다. |
| 요구형 | 단호하고 또렷한 목소리로, 요구 사항을 분명하게 끊어 말합니다. |
| 압박형 | 감정이 격앙된 빠른 말투로, 강하게 압박하듯 말합니다. 다만 소리를 지르지는 않습니다. |

음색은 기본값 `coral`을 사용하되, 배포 환경별 지원 음색 차이로 인한 400 오류에 대비하여 기본 음색(`alloy`)으로의 자동 재시도를 구현하였다.

### 3. 반이중(half-duplex) 턴 제어

스피커로 출력되는 학부모 음성이 마이크로 재입력되어 교사 발화로 오인식되는 **음향 되먹임(acoustic echo)** 문제가 초기 구현에서 관찰되었다. 이를 해결하기 위해 학부모 발화 재생 개시 시점에 음성 인식을 완전 중단(`stopRecording(false)`)하는 반이중 방식을 채택하였다. 이때 잔여 인식 결과를 보존하지 않고 폐기하는데, 이는 재생 직전의 미확정 조각이 다음 턴의 입력을 오염시키는 것을 방지하기 위함이다.

또한 마이크 버튼은 `S.evaluating || S.ended || S.loading || S.speaking` 조건에서 비활성화되어, 상태 전이 중 입력이 불가능하도록 통제된다.

### 4. 이중 상태 모형: 사용자 의도와 엔진 세션의 분리

모바일 웹뷰 환경에서 Web Speech API는 `continuous = true` 설정을 사실상 무시하고, 한 문장 또는 수 초의 무음마다 인식 세션을 자동 종료한다. 이 동작을 그대로 UI에 반영할 경우 학습자가 말하는 도중 녹음 표시가 명멸하여 마이크가 꺼진 것으로 오인하고 버튼을 재차 누르는 문제가 발생하였다.

이에 상태를 두 층으로 분리하였다.

- `S.recording` — **사용자의 녹음 의도**. 버튼 조작으로만 변경된다. 모든 UI 표시는 이 값을 따른다.
- `S.listening` — **엔진 세션의 실제 개폐 상태**. 내부 제어에만 사용된다.

`S.recording`이 참인 동안 세션이 종료되면 UI 변화 없이 조용히 재개방한다. 재개방은 지수적 지연(200ms × 시도횟수, 상한 1,500ms)으로 제어되며, 8회 연속 무음 재시작 시 안내 메시지를 1회 표시하고, 40회 도달 시 녹음을 중단하고 사용자에게 재조작을 요청한다. 이로써 학습자에게는 **단절 없는 하나의 녹음**으로 지각되는 경험을 제공하였다.

### 5. 전사 중복 제거 알고리즘

음성 인식 결과의 중복 누적은 본 개발에서 가장 해결이 어려웠던 기술적 문제였다. 원인은 음성 인식 엔진마다 결과 방출 형태가 상이하다는 데 있다.

- **분할형(데스크톱에서 주로 관찰)**: `event.results`의 각 항목이 서로 겹치지 않는 구간
- **재진술형(안드로이드 웹뷰에서 주로 관찰)**: 각 항목이 앞선 발화 전체를 다시 포함한 뒤 뒤를 늘린 형태

두 형태는 한 세션 안에서 혼재하기도 하며, 명세는 확정 결과 간 비중첩을 보장하지 않는다. 이에 다음 알고리즘(`foldChunks`)을 설계하였다.

1. 누적 인덱스(`resultIndex`)에 의존하지 않고, **매 이벤트마다 결과 목록 전체로 문장을 재구성**한다. 동일 이벤트가 중복 전달되어도 결과가 동일하므로 중복이 원리적으로 발생하지 않는다.
2. 어절 단위로 좌측부터 접으면서, ① 선행 발화를 접두로 포함하며 확장된 조각은 통째로 교체, ② 이미 말미에 포함된 조각은 폐기, ③ 그 외에는 중첩 꼬리만 제거 후 연결한다.
3. 재진술의 **증거 강도**(중첩 어절 수 `span`, 중첩 발생 횟수 `hits`)를 함께 산출한다.
4. 증거가 임계(`span ≥ 2` 또는 `hits ≥ 2`)에 미달하면 접기 결과를 **채택하지 않고** 단순 연결로 되돌린다.

4단계의 보수적 임계 설정은 다음 원칙에 근거한다. **"지우는 것이 남기는 것보다 나쁘다."** 중복은 학습자 눈에 보이므로 수정 가능하지만, 삭제된 발화는 보이지 않은 채 평가 기록까지 훼손한다. 예컨대 "네 네"와 같은 실제 반복 발화를 중복으로 오판하여 제거하는 것은, 중복을 남기는 것보다 연구 데이터 무결성 측면에서 더 큰 손실이다.

추가로, 폐기된 세션에서 지연 도착하는 이벤트가 확정된 본문을 되살리지 못하도록 **세션 일련번호(epoch) 기반 이벤트 무효화**를 적용하였다. 또한 인식 인스턴스를 세션마다 새로 생성하여, 인스턴스 재사용 시 이전 세션 결과가 혼입되는 웹뷰 특유의 현상을 차단하였다.

진단을 위해 URL 질의 문자열 `?voicedebug=1`을 통해 실제 결과 방출 형태를 화면에 표시하는 디버그 모드를 내장하였다.

### 6. 다층 음성 폴백 및 접근성

| 실패 지점 | 대체 동작 |
| --- | --- |
| 서버 TTS 실패 | 브라우저 내장 `SpeechSynthesis`로 재생 |
| 브라우저 TTS 실패 | 텍스트만 표시하고 안내 메시지 1회 노출 |
| 음성 인식 미지원 브라우저(iOS Safari 등) | 안내 후 텍스트 입력으로 연습 지속 |
| 마이크 권한 차단 | 권한 허용 방법 안내 후 텍스트 입력 유도 |
| 자동재생 정책 차단 | 사용자 제스처 시점에 무음 클립을 선재생하여 오디오 컨텍스트 사전 확보 |
| 브라우저 TTS의 `onend` 미호출 | 텍스트 길이 비례 상한 타이머(최대 30초)로 강제 종료 처리 |

브라우저 TTS 사용 시 한국어 음성 목록이 비동기로 채워지는 문제에 대응하여, 음성 목록 준비를 최대 1.5초간 대기한 후 발화하도록 구현하였다.

### 7. 학습 몰입 보호를 위한 부분 렌더링

전체 화면 재렌더링은 녹음 중 입력창의 내용과 커서를 흔들어 학습자의 발화를 방해한다. 이에 음성 상태 변화(`paintVoiceState`)와 즉시 피드백 갱신(`paintFeedback`)은 해당 DOM 요소만 갱신하도록 분리하였다. 또한 학부모 발화마다 개별 재생/정지 버튼을 제공하여, 학습자가 놓친 발화를 임의 시점에 재청취할 수 있도록 하였다.

---

## Ⅳ. 민원 대응 역량 평가 체계 설계 결과

### 1. 평가 준거의 구조

민원 대응 역량을 **3개 영역 14개 수행요소**로 조작화하였다. 영역 구분은 민원 대응 과정에서 요구되는 역량의 성격 차이에 따른 것이다.

| 영역 | 요소 수 | 역량의 성격 |
| --- | ---: | --- |
| Ⅰ. 의사소통 | 4 | 정보의 수신과 전달에 관한 역량 |
| Ⅱ. 갈등 완화 | 4 | 정서적·관계적 국면의 조절 역량 |
| Ⅲ. 절차적 대응 | 6 | 제도적 판단과 경계 설정 역량 |

**14개 수행요소의 조작적 정의**

| 영역 | 요소 | 조작적 정의 |
| --- | --- | --- |
| Ⅰ. 의사소통 | 요구 파악 | 학부모의 핵심 요구와 쟁점을 경청하고 확인한다 |
| | 사실 확인 | 추측 전에 필요한 사실관계를 질문하고 확인한다 |
| | 공감적 표현 | 감정을 인정하되 성급히 동의하지 않는다 |
| | 명료한 설명 | 상황·판단·가능한 조치를 이해하기 쉽게 설명한다 |
| Ⅱ. 갈등 완화 | 감정적 상황 대응 | 감정이 고조되어도 안정적인 태도를 유지한다 |
| | 비대립적 의사소통 | 반박보다 문제 해결에 초점을 둔다 |
| | 쟁점 조정 | 감정과 해결 사안을 구분해 해결 가능한 쟁점으로 전환한다 |
| | 갈등 확대 방지 | 갈등을 자극하는 표현을 피하고 악화를 조절한다 |
| Ⅲ. 절차적 대응 | 사안 판단 | 교사가 직접 대응할 수 있는 사안인지 판단한다 |
| | 대응 범위 설정 | 무리한 약속 없이 대응 가능한 범위를 설명한다 |
| | 후속 절차 안내 | 확인·보고·회신 등 이후 절차를 안내한다 |
| | 경계 설정 | 부당 요구·폭언 등의 허용 범위를 분명히 설정한다 |
| | 이관·보고 판단 | 관리자 또는 학교 민원대응체계 이관을 적절히 판단한다 |
| | 대응 중단 판단 | 정상 응대가 불가능할 때 적절히 대응을 종료한다 |

**설계상의 특징.** Ⅲ영역에 6개 요소를 배치하여 절차적 대응을 가장 세분화하였다. 이는 앞서 코퍼스 분석에서 확인된 민원 분포(연락/근무시간 34.4%, 폭언/협박 9.8%)를 반영한 것으로, 실제 민원의 상당 부분이 **공감이나 설명으로 해소되는 문제가 아니라 대응 범위와 경계의 설정을 요구하는 문제**임을 준거에 반영한 결과이다. 특히 '경계 설정', '이관·보고 판단', '대응 중단 판단'은 기존의 민원 대응 연수가 상대적으로 소홀히 다루어 온 요소로서, 교사 개인의 무한 감내를 전제하지 않는 대응을 학습 대상으로 명시화하였다는 의의를 갖는다.

### 2. 척도 및 채점 규칙

**① 4점 행동 준거 척도**

| 점수 | 행동 기술 |
| ---: | --- |
| 4 | 일관되고 적절한 수행 |
| 3 | 대체로 적절하나 일부 불명확 |
| 2 | 부분적 인식 및 수행 |
| 1 | 수행되지 않음 또는 부적절 |

**② 관찰 가능성(applicable) 판정.** 시뮬레이션 대화는 유한하므로, 특정 요소가 관찰될 기회 자체가 없을 수 있다(예: 학부모가 부당 요구를 하지 않은 대화에서의 '경계 설정'). 이에 각 요소에 `applicable` 불리언을 부여하고, `false`인 요소는 총점 산출에서 제외한다.

다만 이 장치가 **관대화 편향의 통로가 되지 않도록** 다음 규칙을 명시하였다. "해당 없음은 편의상 주지 마세요. 대화에서 기대 가능한 요소인데 드러나지 않았다면 `applicable:true`, 1점으로 평가하세요." 즉 '기회의 부재'와 '수행의 부재'를 엄격히 구분한다.

**③ 환산 총점.**

```
환산 총점 = (관찰 가능 요소들의 평균 점수) × 14
```

이론적 범위는 14점~56점이며, 만점 56점(= 4점 × 14요소)으로 표시된다. 관찰 요소 수가 대화마다 달라지므로 단순 합산이 아닌 **평균 기반 환산**을 채택하여, 대화 길이나 상황 특성에 따른 총점의 비교 불가능성을 완화하였다. 결과 화면에는 환산 총점과 함께 **관찰 요소 수**를 병기하여, 점수의 산출 근거를 학습자가 확인할 수 있도록 하였다.

**④ 근거 의무화.** 모든 요소에 대해 `evidence` 문장(해당 점수의 구체적 대화 근거 또는 미흡 사유)을 반드시 산출하도록 스키마 수준에서 강제하였다. 이는 평가를 점수 통보가 아닌 **성찰 자료**로 기능하게 하려는 설계 의도이며, 학습자가 평가 결과의 타당성을 스스로 검토할 수 있는 조건을 제공한다.

### 3. 평가 실시 요건 통제

**① 최소 대화량 요건.** 교사 발화 4회 미만인 대화는 평가할 수 없도록 하였다. 이는 클라이언트(평가 버튼 비활성화)와 서버(HTTP 400 응답) **양단에서 이중 검증**된다. 짧은 대화에 대한 평가는 준거의 상당수를 관찰 불가로 만들어 결과의 해석 가능성을 훼손하기 때문이다.

**② 대화 종료 판정.** AI 학부모가 대화 종료를 판단하더라도, 서버에서 `ended = (teacherTurns >= 4) && 모델 판단`으로 재계산하여 조기 종료를 차단한다. 종료 시에는 학부모 발화 안에 "대화가 마무리되었습니다."라는 문장이 자연스럽게 포함되도록 하여, 학습자가 종료 시점을 명확히 인지하도록 하였다.

**③ 대화 중 점수 노출 금지.** 대화 생성 프롬프트에 "metCriteria에는 바로 직전 교사 발화에서 관찰된 수행요소만 넣습니다. **점수는 절대 말하지 않습니다.**"를 명시하여, 진행 중 점수 정보가 학습자의 후속 발화를 왜곡하는 것을 방지하였다.

---

## Ⅴ. AI 평가 및 피드백 체계 설계 결과

### 1. 이원적 피드백 구조

피드백은 시점과 기능을 달리하는 두 층위로 설계되었다.

| 구분 | 형성적 피드백 | 총괄적 평가 |
| --- | --- | --- |
| 엔드포인트 | `/api/turn-feedback` | `/api/evaluate` |
| 시점 | 교사 발화 직후(매 턴) | 대화 종료 후(1회) |
| 대상 | **직전 교사 발화 1개** | 대화 전체 |
| 산출 | 관찰 요소 · 다음 제안 요소 · 2문장 이내 코멘트 | 14요소 점수·근거, 강약점, 종합 의견, 환산 총점 |
| 기능 | 수행 중 조정(in-action) | 수행 후 성찰(on-action) |
| 실패 시 | 무음 처리, 학습 지속 | 오류 안내 + 재시도 버튼, 대화 기록 보존 |

### 2. 형성적 피드백 설계

**① 평가 대상의 국소화.** 초기 구현에서는 매 턴 유사한 일반론이 반복 산출되는 문제가 있었다. 원인은 프롬프트가 대화 전체를 제공하면서 평가 대상을 특정하지 않은 데 있었다. 이에 (가) 최근 8개 발화를 맥락으로 제공하되, (나) **직전 교사 발화를 별도 항목으로 분리 제시**하고, (다) "아래 대화의 마지막 교사 발화 한 개만 평가합니다. 이전 발화나 대화 전체를 평가하지 마세요"를 명시하였다. 또한 "교사가 방금 한 말에서 실제로 쓴 표현을 짚어 주고", "일반론이 아니라 이 발화에만 해당하는 내용이어야 한다"는 제약으로 구체성을 강제하였다.

**② 인지 부하 통제.** 대화 중 읽는 피드백이므로 `met` 최대 2개, `next` 최대 2개, `message` 2문장 이내로 상한을 두었다. 점수·등급·총평은 산출하지 않는다.

**③ 추측 금지.** "근거가 없으면 빈 배열로 둡니다. 추측으로 채우지 마세요"를 명시하여, 관찰되지 않은 요소를 관찰된 것으로 보고하는 허위 긍정을 억제하였다.

**④ 학습 흐름 보호를 위한 비동기 설계.** 형성적 피드백은 학부모 응답 요청과 **병렬로** 발행되어 응답 지연을 유발하지 않는다. 연속 전송 시 이전 요청의 응답이 뒤늦게 도착해 최신 피드백을 덮어쓰는 경쟁 조건은 요청 순번(`feedbackSeq`) 비교로 차단하였다. 또한 이 엔드포인트는 **실패해도 HTTP 200과 `degraded: true`를 반환**한다. 피드백의 실패가 대화 연습을 중단시켜서는 안 된다는 원칙에 따른 설계이다.

### 3. 총괄 평가 설계

**① 단일 호출 통합 설계(설계 진화).** 초기 설계는 1차 요소 채점과 2차 검토를 순차 호출하는 2단계 구조였다. 그러나 이 구조는 서버리스 함수의 실행 시간 제한을 초과하여 함수가 강제 종료되고, 브라우저가 JSON 대신 플랫폼 오류 페이지를 수신하는 문제를 야기하였다. 이에 **요소 채점과 서술 피드백을 단일 스키마에 담아 1회 호출**하는 구조로 통합하였다. 그 결과 실패 지점이 단일화되고 호출 비용이 절반으로 감소하였다. 평가 요청에는 별도의 타임아웃 예산(기본 20초, `OPENAI_EVAL_TIMEOUT_MS`)을 적용하여, 플랫폼이 함수를 종료하기 전에 프로그램이 먼저 포기하고 해석 가능한 JSON 오류를 반환하도록 하였다.

**② 구조적 출력 강제.** 모든 AI 호출에 JSON Schema(`strict: true`, `additionalProperties: false`)를 적용하였다. 특히 수행요소명은 **14개 값의 열거형(enum)으로 제약**하여, 모델이 준거에 없는 요소명을 생성하는 것을 원천 차단하였다. 점수는 `integer, minimum: 1, maximum: 4`로 스키마 수준에서 제한된다.

**③ 이중 정규화.** 스키마 제약에도 불구하고 결측·범위 이탈이 발생할 가능성에 대비하여, 서버(`normalizeCriteria`)와 클라이언트(`normalizeEvaluation`) 양단에서 정규화를 수행한다. 14개 요소를 항상 완전 집합으로 재구성하고, 점수는 1~4로 클램프하며, 근거 문장이 없으면 기본 문구로 대체한다. 이로써 결과 화면은 어떤 응답에 대해서도 일관된 구조를 유지한다.

### 4. 모델 운용 설계

과제 특성에 따라 모델을 계층화하고, 전 계층에 대체 모델을 지정하였다.

| 용도 | 기본 모델 | 환경변수 | 선정 근거 |
| --- | --- | --- | --- |
| 학부모 대화 | `gpt-4.1` | `OPENAI_CHAT_MODEL` | 구어체 자연스러움이 핵심. 추론 모델보다 대화형 모델이 적합 |
| 시나리오 생성 | `gpt-4.1-mini` | `OPENAI_SCENARIO_MODEL` | 구조화 산출 중심, 비용 효율 |
| 형성적 피드백 | `gpt-4.1-mini` | `OPENAI_TURN_FEEDBACK_MODEL` | 짧은 지연이 요구됨 |
| 총괄 평가 | `gpt-4.1-mini` | `OPENAI_PRIMARY_EVAL_MODEL` | 스키마 준수 안정성 |
| 음성 합성 | `gpt-4o-mini-tts` | `OPENAI_TTS_MODEL` | 낭독 지시문 지원 |

**대체 모델 전략(`createWithFallback`).** 1차 모델 실패 시 `gpt-4.1-mini`로 재시도하되, 다음 경우는 즉시 포기한다. (가) 인증 실패 — 모델을 바꾸어도 결과가 동일함, (나) 타임아웃 — 재시도 시 함수 실행 제한 초과가 확실함. 반면 연결 즉시 단절과 5xx 응답은 재시도 가치가 있으므로 지수적 지연을 두어 재시도한다. 이 구분은 무의미한 재시도로 인한 지연 누적을 방지하기 위한 것이다.

**추론 모델 대응.** 추론 계열 모델(`gpt-5`, `o1`, `o3`, `o4`)은 내부 추론 토큰도 출력 예산에서 차감하므로, 짧은 발화를 요구하는 호출에서 추론만 수행하고 본문이 비는 현상이 발생한다. 이에 추론 모델 감지 시 1,200토큰의 여유분을 자동 가산하도록 구현하였다.

### 5. 결과 표상 설계

결과 화면은 다음 5개 구획으로 구성된다.

1. **종합 요약 및 환산 총점** — 총점(56점 만점)과 관찰 요소 수 병기
2. **영역별 평균** — Ⅰ·Ⅱ·Ⅲ 영역별 평균(4점 만점)과 관찰 요소 수, 관찰 요소가 없으면 '해당 없음' 표기
3. **종합 의견 및 강점·보완점** — 각 2~4개 항목
4. **14개 요소별 근거 표** — 영역·요소·점수·근거 문장의 전체 목록
5. **대화 기록 전문** — 평가 근거의 검증 가능성 확보

또한 결과 전체를 A4 규격 PDF로 저장하는 기능(`html2canvas` + `jsPDF`, 다면 분할 처리)을 제공하여, 학습자가 반복 시도 간 결과를 비교하거나 연수 산출물로 보관할 수 있도록 하였다.

### 6. 평가의 위상에 관한 고지

프로그램은 타이틀 화면에서 **"AI 평가는 학습 피드백이며 객관적 역량 판정이 아닙니다"**를 명시한다. 이는 AI 기반 평가의 신뢰도가 아직 충분히 검증되지 않은 상태에서, 학습자가 결과를 자기 역량에 대한 확정적 판정으로 오인하지 않도록 하기 위한 연구윤리적 조치이다.

---

## Ⅵ. 최종 프로그램 구현 결과

### 1. 구현 산출물

| 구분 | 파일 | 규모 | 역할 |
| --- | --- | ---: | --- |
| 진입점 | `index.html` | 21행 | 정적 진입점, 페르소나 이미지 선로딩, PDF 라이브러리 지연 로드 |
| 클라이언트 | `assets/app.js` | 482행 | 화면 전이, 음성 상호작용, 상태 관리, 결과 표상 |
| 스타일 | `assets/styles.css` | 1,690행 | 반응형 UI, 유리질감 카드 레이아웃 |
| 공통 모듈 | `netlify/functions/_shared.js` | 230행 | AI 호출, 타임아웃·재시도, 오류 변환, 저장 |
| 대화 | `netlify/functions/chat.js` | 264행 | 첫 발화 및 후속 턴 생성 |
| 시나리오 | `netlify/functions/random-situation.js` | 238행 | 사례 기반 시나리오 생성 |
| 평가 | `netlify/functions/evaluate.js` | 51행 | 14요소 총괄 평가 |
| 피드백 | `netlify/functions/turn-feedback.js` | 66행 | 턴 단위 형성적 피드백 |
| 음성 | `netlify/functions/speak.js` | 72행 | 페르소나별 음성 합성 |
| 진단 | `netlify/functions/health.js` | 86행 | 외부 의존성 상태 점검 |
| 저장 | `netlify/functions/save-message.js`, `save-situation.js` | 49행 | 기록 저장(확장 지점) |
| 실시간 | `netlify/functions/realtime-session.js` | 48행 | 실시간 음성 세션 발급(확장 지점) |
| 코퍼스 | `netlify/functions/_complaint_cases.js` | 14,548행 | 비식별 민원 사례 2,077건 |
| 도구 | `tools/extract_complaint_cases.py` | — | PDF → 코퍼스 추출 파이프라인 |
| 스키마 | `supabase.sql` | 68행 | 3개 테이블 및 권한 정의 |
| 자원 | `assets/personas/*`, `assets/cards/*` | 15개 | 페르소나 이미지(webp/png), 선택 카드(svg) |

### 2. API 엔드포인트 구현 현황

| 경로 | 메서드 | 현행 흐름 사용 | 비고 |
| --- | --- | :---: | --- |
| `/api/chat` | POST | ● | 첫 발화(`initial`)와 후속 턴 분기 처리 |
| `/api/speak` | POST | ● | 페르소나별 낭독 지시문 적용 |
| `/api/random-situation` | POST | ● | 페르소나 값을 수신하지 않음(직교 원칙) |
| `/api/turn-feedback` | POST | ● | 실패 시에도 200 반환 |
| `/api/evaluate` | POST | ● | 교사 발화 4회 미만 시 400 반환 |
| `/api/health` | GET | ○ | 운영 진단용, 키 값 자체는 응답에 미포함 |
| `/api/save-message` | POST | ○ | 대화 저장은 현재 `chat.js` 내부에서 수행 |
| `/api/save-situation` | POST | ○ | 시나리오 저장 확장 지점 |
| `/api/realtime-session` | POST | ○ | 실시간 음성 대화 확장 지점(시간당 10회 제한 구현) |

### 3. 데이터 저장 구조

| 테이블 | 저장 내용 | 주요 컬럼 |
| --- | --- | --- |
| `simulation_messages` | 대화 전사 | `session_id`, `attempt_id`, `role`, `content`, `input_mode`, `teacher_type`, `school_level`, `parent_type`, `situation` |
| `simulation_evaluations` | 평가 결과 | `session_id`, `score`, `scaled_score`, `summary`, `strengths`, `improvements`, `conversation`(JSONB) |
| `simulation_situations` | 시나리오 | `session_id`, `situation_mode`, `situation`, `situation_context`, `privacy_acknowledged_at` |

전 테이블에 행 수준 보안(RLS)을 활성화하고, 접근 권한을 `service_role`에만 부여하였다. 스키마에는 **"Audio recordings are deliberately not stored; only transcript text and AI text are retained."**를 주석으로 명시하여, 음성 원본 미저장 원칙을 설계 문서 수준에서 고정하였다.

### 4. 배포 및 운영 구성

- **호스팅**: Netlify (정적 자원 + 서버리스 함수)
- **빌드**: 별도 빌드 단계 없음, `publish = "."`, 함수 번들러 `esbuild`
- **캐시 제어**: `/assets/*`에 `Cache-Control: public, max-age=0, must-revalidate` 적용. 스크립트가 버전 질의 문자열 없이 참조되므로, 재검증을 강제하지 않으면 배포된 수정이 반영되지 않는 문제를 해결하기 위함
- **환경변수 13종**: `OPENAI_API_KEY`, `OPENAI_MODEL`, `OPENAI_CHAT_MODEL`, `OPENAI_SCENARIO_MODEL`, `OPENAI_TURN_FEEDBACK_MODEL`, `OPENAI_PRIMARY_EVAL_MODEL`, `OPENAI_TIMEOUT_MS`, `OPENAI_EVAL_TIMEOUT_MS`, `OPENAI_TTS_MODEL`, `OPENAI_TTS_VOICE`, `OPENAI_REALTIME_MODEL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`
- **비밀값 관리**: `SUPABASE_SERVICE_ROLE_KEY`와 `OPENAI_API_KEY`는 서버 환경변수로만 관리하며, 저장소와 클라이언트 번들에 포함하지 않음

### 5. 오류 처리 및 관측 가능성

운영 중 발생 가능한 실패를 사용자 조치 가능성 기준으로 분류하여, 화면에는 조치 가능한 문장만 노출하고 원문은 서버 로그에만 기록하도록 하였다.

| 오류 코드 | HTTP | 화면 안내 | 재시도 |
| --- | ---: | --- | :---: |
| `auth` | 503 | AI 서비스 인증 실패, 환경변수 확인 안내 | × |
| `rate` | 429 | 사용량 한도 도달, 잠시 후 재시도 안내 | ○ |
| `network` | 504 | 연결 실패 사유 명시(타임아웃/DNS/연결 거부 구분) | 조건부 |
| `upstream` | 502 | AI 서비스 일시 무응답 안내 | ○ |

또한 서버리스 함수가 실행 제한으로 강제 종료되면 플랫폼이 JSON이 아닌 HTML 오류 페이지를 반환하여 `Unexpected token '<'` 오류가 화면에 노출되는 문제가 있었다. 이에 클라이언트에 비-JSON 응답 감지 로직(`describeNonJson`)을 두어, HTTP 상태에 따른 해석 가능한 한국어 안내로 변환하도록 하였다.

`/api/health`는 OpenAI와 Supabase 각각에 대한 연결 점검 결과, 환경변수 설정 상태(값 자체는 제외, 형식 이상·공백 혼입 여부 포함), 응답 지연을 반환하여, 배포 환경에서 로그 접근 없이 장애 지점을 식별할 수 있도록 하였다.

### 6. 개인정보 보호 및 연구윤리 구현

| 조치 | 구현 위치 |
| --- | --- |
| 음성 원본 미저장, 전사 텍스트만 보관 | 클라이언트 설계 + `supabase.sql` 주석 |
| 사전 고지(입력 방식, 활용 범위, 개인정보 미입력, 평가의 위상) | 타이틀 화면 고지 카드 4개 항목 |
| 개인정보 미입력 안내 | 직접 입력 화면 및 입력창 안내문 |
| 생성 시나리오 비식별화 | 프롬프트 규칙 + `ensureChildName` 후처리 |
| 학생 명칭 고정(`금쪽이`) | 시나리오 생성·대화 생성 양쪽에 적용 |
| 폭력적 표현의 교육적 순화 | 시나리오 생성 및 전 페르소나 공통 규칙 |
| 서비스 롤 키의 서버 전용 관리 | Netlify 환경변수, RLS 활성화 |
| 연구 참여 설문 연계 | 결과 화면 설문 링크 |

### 7. 개발 과정의 반복적 개선

프로그램은 형성적 개발 방식으로 구축되었으며, 저장소 이력에 기록된 주요 개선 국면은 다음과 같다.

| 국면 | 문제 | 해결 |
| --- | --- | --- |
| 페르소나 일관성 | 선택한 유형이 대화에 드러나지 않음 | 9차원 페르소나 구조화, 유형별 말투 지침 서버 주입, 안정적 페르소나 ID 도입 |
| 첫 발화 품질 | 상황 설명문을 낭독하여 지문처럼 들림 | 첫 발화 전용 프롬프트 분리, 긍정·부정 예시 병기, 상황문 인용 금지 |
| 역할 이탈 | 모델이 교사 역할로 전환하거나 3인칭 해설 | 역할 고정 제약 중첩, 금지 표현 명시 |
| 음성 루프 단절 | 학부모 발화 후 마이크가 재개방되지 않음 | 반이중 턴 제어 및 이중 상태 모형 도입 |
| 전사 중복 | 엔진별 결과 방출 형태 차이로 문장 중복 누적 | `foldChunks` 접기 알고리즘 및 신뢰 임계 도입 |
| 평가 시간 초과 | 2단계 평가 호출이 함수 실행 제한 초과 | 단일 스키마 통합 호출로 재설계, 전용 타임아웃 예산 부여 |
| 오류 가시성 | 원인 불명의 500 오류 | 오류 코드 체계화, `/api/health` 진단 엔드포인트 신설 |
| 저장 실패 전파 | 기록 저장 실패가 대화를 중단시킴 | 저장 계층에서 예외 흡수, 실패해도 학습 지속 |

검증 산출물로 화면별 스크린샷 6종(`verification-*.png`: 타이틀, STEP 1·2, 시뮬레이션 정적·동적, 모바일)을 저장소에 보관하고 있다.

### 8. 프로그램의 특징 요약

본 연구에서 개발한 프로그램의 설계·개발상 특징은 다음 다섯 가지로 요약된다.

1. **실증 자료 기반 시나리오 생성.** 2,077건의 실제 민원 사례를 기계 판독 가능한 코퍼스로 구축하고, 이를 비식별 재구성하여 시나리오를 생성함으로써 진정성과 연구윤리를 동시에 확보하였다.

2. **페르소나-시나리오 직교 설계.** 학부모의 성격과 민원 사건을 독립 차원으로 분리하여, 사건 유형별 매뉴얼이 아닌 관계적 대응 원리의 학습을 유도하였다.

3. **이중 표상에 의한 능동적 사실 확인 유도.** 학습자 표시용 요약과 AI 전용 내부 맥락을 비대칭적으로 구성하여, '사실 확인'이라는 수행이 실제로 요구되고 관찰되는 학습 조건을 만들었다.

4. **음성 우선 상호작용의 실환경 구현.** 브라우저·기기별로 상이한 음성 인식 동작을 이중 상태 모형과 보수적 중복 제거 알고리즘으로 흡수하고, 다층 폴백을 통해 실패 상황에서도 학습이 중단되지 않도록 하였다.

5. **근거 기반 이원 평가 체계.** 3영역 14요소 루브릭에 관찰 가능성 판정과 근거 문장 의무화를 결합하고, 형성적 피드백과 총괄 평가를 시점·기능에 따라 분리하여 수행 중 조정과 수행 후 성찰을 모두 지원하였다.

### 9. 제한점 및 후속 과제

**① 평가 신뢰도의 미검증.** 본 연구에서 설계한 14요소 루브릭에 대해, AI 평가 결과와 교육 전문가 평가 간 일치도(예: 급내상관계수, 가중 카파)는 아직 산출되지 않았다. AI 평가를 학습 피드백으로 한정하여 고지하고 있는 것은 이 때문이며, 후속 연구에서 동일 대화 표본에 대한 전문가 병렬 채점을 통한 준거 타당도 및 평정자 간 신뢰도 검증이 요구된다.

**② 음성 인식의 플랫폼 의존성.** 현행 구현은 브라우저 내장 Web Speech API에 의존하므로 Chrome·Edge 계열에서만 완전한 음성 경험이 제공되며, iOS Safari 등에서는 텍스트 입력으로 대체된다. 실시간 음성 대화 API를 사용하는 대체 경로(`/api/realtime-session`)가 구현되어 있으나 현행 학습 흐름에는 연결되어 있지 않다. 플랫폼 독립적 음성 경험 확보가 후속 과제이다.

**③ 검색 기반 생성(RAG)의 미완성.** 현재 시나리오 생성은 무작위 표집 기반이며, `RAG_PLAN.md`에 설계된 pgvector 임베딩 기반 유사 사례 검색은 미구현 상태이다. 이를 도입할 경우 학교급·주제·학습자 이력에 정합적인 시나리오 선택이 가능해질 것으로 기대된다.

**④ 학습 이력의 종단적 활용 미구현.** 데이터 스키마에는 재시도 회차(`attempt_id`, `attempt_number`)를 저장할 구조가 마련되어 있으나, 반복 시도 간 성장 추이를 학습자에게 제시하는 기능은 구현되지 않았다. 또한 평가 점수 저장 컬럼(`score`)이 0~100 척도로 정의되어 있어 현행 56점 환산 척도와 정합하지 않으므로(`scaled_score` 컬럼 미사용), 후속 데이터 분석 이전에 스키마 정비가 필요하다.

**⑤ 문서 최신화.** `APPLIED_PROMPTS.md`에 기록된 프롬프트는 7개 평가 영역 체계에 기반한 이전 판으로, 현행 3영역 14요소 체계 및 최종 프롬프트와 일치하지 않는다. 연구 재현성 확보를 위해 최종 프롬프트 전문의 문서화가 요구된다.

---

*본 문서는 저장소 `ryukoyat-cmyk/simulation`의 최종 구현 상태를 근거로 작성되었다.*
